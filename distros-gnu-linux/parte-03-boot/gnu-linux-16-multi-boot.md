# Multi boot

Llamado **multi boot** o **dual boot**: Esta es una capacidad que tienen algunos gestores de arranque. **GRUB** es uno de esos.

Esta función del **multi-boot/dual-boot**. Permite gestionar varios gestores de arranque en un mismo gestor de arranque.

**GRUB** por ejemplo, te permite tener un menu de selección donde puedes seleccionar entre windows o distro linux.

Gracias a esta función del dual **multi-boot/dual-boot**. Se pueden tenner varios sistemas operativos instlados en un mismo disco duro.

Otro ejemplo; el gestor de arranque de windows 10, permite la seleccion de diferentes windows instalados en el mismo disco duro.

> Nota: Esta función del multi boot no solo sirve para seleccionar OS instalados en el mismo disco duro, sino que tambien sirve para selccionar OS en otros discos duros. Solo que no lo mencione porque eso ya se puede hacer desde la BIOS.