# Hacer objeto vinculado/linked local
En modo objeto, seleccionamos el objeto o malla vinculada. Buscamos la opción que esta en el menu superior, que dice `Object`, esta al lado derecho de otras opciónes como `Add`, `Select`, `View`.

Buscamos la opción relaciones `Relations`/`Make Local`, `Selected Object, Data and Materials`. Checa entre las opciones lo mas acertado para tu caso.

Eso es todo, sencillo, pero si no sabes, pos como que ta canijo buscar solución a esto.