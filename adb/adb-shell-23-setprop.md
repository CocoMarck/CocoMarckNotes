[Enlace](https://adbshell.com/commands/adb-shell-setprop)

# adb shell setprop
Servicio de propiedad establecida. `setprop <key> <value>`

**STEP 1**
```bash
adb shell
```

**STEP 2**
``` bash
setprop service.adb.tcp.port 5555
```
También ver `getprop`.
