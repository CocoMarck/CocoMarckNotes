https://adbshell.com/commands/adb-shell-ip


# adb shell ip
Obtener la dirección IP wlan0 (Wi-Fi)
```bash
adb shell ip addr show wlan0
```

Tabla de rutas
```bash
adb shell ip route
```

Tabla ARP 
```bash
adb shell ip neighbour
```