# Modulo de una división

## Ejemplos de como obtener el modulo de una división

### Sacar modulo:

$Dividendo -(Cociente \cdot Divisor) = ModuloDeDivisión$

**El modulo de quince entre diez es cinco:**
```
    1.5
10/15
   10
    5 <-- Modulo, resto de división
```

> El cociente es $1$, ya que el valor de la división es $1.5$, tomamos unicamente como cociente el valor entero, no el flotante.

> $15 -(10 \cdot 1) = 5$ `<-- Modulo, resto de división`

    

**El modulo de Diez entre quince es diez:**
```
    0.6
15/10 0
    9 0
    1 0 <-- Modulo, resto de división
```

> El cociente es $0$, ya que el valor de la división es $0.6$, tomamos unicamente como cociente el valor entero, no el flotante. 

> $10 -(15 \cdot 0) = 10$ `<-- Modulo, resto de división`
   


**El modulo de Treinta entre veinte es diez:**
```
    1.5
20/30
   10 <-- Modulo, resto de división
```
   
> El cociente es $1$, ya que el valor de la división es $1.5$, tomamos unicamente como cociente el valor entero, no el flotante.

$30 -(20 \cdot 1) = 10$ `<-- Modulo, resto de división`